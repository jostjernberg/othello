package kth.game.othello.board;


public class BoardTest {
	/*
	 * @Test public void testNodeListConsistency() { InternalBoard b = new ClassicBoard(8, 8); Assert.assertEquals(8 *
	 * 8, b.getNodes().size()); Iterator<Node> nodeIt = b.getNodes().iterator();
	 * 
	 * for (int r = 0; r < 8; r++) { for (int c = 0; c < 8; c++) { Node n = nodeIt.next(); Assert.assertEquals(c,
	 * n.getXCoordinate()); Assert.assertEquals(r, n.getYCoordinate()); } } }
	 */
}
